{
  "name": "headless-cms",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "server:install": "cd server && npm install",
    "server:start": "cd server && npm start",
    "admin-ui:install": "cd admin-ui && npm install",
    "admin-ui:start": "cd admin-ui && npm start",
    "consumer-server:install": "cd consumer-server && npm install",
    "consumer-server:start": "cd consumer-server && npm start",
    "consumer-admin-ui:install": "cd consumer-admin-ui && npm install",
    "consumer-admin-ui:start": "cd consumer-admin-ui && npm run start",
    "consumer-admin-ui:build:watch": "cd consumer-admin-ui && npm run build:watch",
    "postinstall": "npm-run-all server:install consumer-server:install consumer-admin-ui:install admin-ui:install",
    "start": "npm-run-all -p server:start consumer-server:start consumer-admin-ui:start admin-ui:start"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/alexbechmann/headless-cms.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/alexbechmann/headless-cms/issues"
  },
  "homepage": "https://github.com/alexbechmann/headless-cms#readme",
  "devDependencies": {
    "npm-run-all": "^4.1.3"
  }
}
