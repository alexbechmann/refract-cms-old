{
  "name": "@refract-cms/domain",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "scripts": {
    "build": "npm-run-all core:build server:build",
    "start": "npm-run-all -p core:watch server:watch",
    "core:build": "tsc --outDir ../core/src/generated",
    "core:watch": "tsc -w --outDir ../core/src/generated",
    "server:build": "tsc --outDir ../server/src/generated",
    "server:watch": "tsc -w --outDir ../server/src/generated"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "typescript": "^3.0.1"
  }
}
